import { Roboto, Inter } from 'next/font/google'

export const roboto = Roboto({
    weight: '400',
    subsets: ['latin']
})

export const inter = Inter({
    weight: '300',
    subsets: ['latin']
})